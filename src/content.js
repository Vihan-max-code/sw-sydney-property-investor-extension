(function(){"use strict";let API_BASE="http://localhost:3001";if(typeof chrome!=="undefined"&&chrome.storage?.local){chrome.storage.local.get("apiBase",(d)=>{if(d.apiBase)API_BASE=d.apiBase})}const site=location.hostname.includes("domain")?"domain":location.hostname.includes("realestate")?"rea":"other";let lastUrl="";let suburbCache={};const SUBURB_PATTERNS=[{regex:/macquarie[\-\+%20\s]*fields/i,name:"macquarie fields",postcode:"2564"},{regex:/minto(?![\-\s]heights)/i,name:"minto",postcode:"2566"},{regex:/leumeah/i,name:"leumeah",postcode:"2560"},{regex:/glenfield/i,name:"glenfield",postcode:"2167"},{regex:/holsworthy/i,name:"holsworthy",postcode:"2173"},{regex:/east[\-\+%20\s]*hills/i,name:"east hills",postcode:"2213"}];const POSTCODE_MAP={"2564":"macquarie fields","2566":"minto","2560":"leumeah","2167":"glenfield","2173":"holsworthy","2213":"east hills"};function detectSuburbFromUrl(url){const u=(url||location.href).toLowerCase();for(const p of SUBURB_PATTERNS){if(p.regex.test(u))return{name:p.name,postcode:p.postcode}}for(const[pc,name]of Object.entries(POSTCODE_MAP)){if(u.includes(pc))return{name,postcode:pc}}return null}function isSearchPage(url){const u=(url||location.href).toLowerCase();if(!detectSuburbFromUrl(u))return false;if(site==="domain")return u.includes("/sale/")&&!u.match(/\d{9,}/);if(site==="rea")return(u.includes("/buy/")||u.includes("/rent/"))&&(u.includes("list")||u.includes("/in-"));return false}function isListingPage(url){const u=(url||location.href).toLowerCase();if(!detectSuburbFromUrl(u))return false;if(site==="domain")return!!u.match(/\d{9,}/);if(site==="rea")return u.includes("/property-");return false}function sendMsg(msg){return new Promise((resolve)=>{const timeout=setTimeout(()=>resolve(null),5000);try{chrome.runtime.sendMessage(msg,(resp)=>{clearTimeout(timeout);if(chrome.runtime.lastError){resolve(null);return}resolve(resp?.data||null)})}catch(e){clearTimeout(timeout);resolve(null)}})}async function directFetch(url,options){try{const resp=await fetch(url,{...options,mode:"cors"});return resp.ok?await resp.json():null}catch(e){return null}}async function fetchSuburbData(name,postcode){const key=name.toLowerCase();if(suburbCache[key]&&(Date.now()-suburbCache[key]._t<3600000))return suburbCache[key];let data=await sendMsg({action:"getSuburb",name,postcode:postcode||""});if(!data)data=await directFetch(`${API_BASE}/api/suburb/${name.replace(/\s+/g,"-")}?postcode=${postcode||""}`);if(data){data._t=Date.now();suburbCache[key]=data}return data}async function fetchAnalysis(suburb,postcode,price,beds,baths,landSize,propertyType){const payload={suburb,postcode,price,beds,baths,landSize,propertyType};let data=await sendMsg({action:"analyse",payload});if(!data)data=await directFetch(`${API_BASE}/api/analyse`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});return data}function parsePrice(s){if(!s)return null;const m=s.match(/\$\s*[\d]{1,3}(?:,\d{3})*(?:\.\d+)?/g);if(!m)return null;const v=m.map(x=>parseInt(x.replace(/[$\s,]/g,""))).filter(n=>n>=100000&&n<=5000000);return!v.length?null:v[0]}function fmt(n){return n>=1e6?"$"+(n/1e6).toFixed(2)+"M":n>=1e3?"$"+(n/1e3).toFixed(0)+"k":"$"+n}function extractFeatures(t){let beds=0,baths=0,cars=0,land=0,type="House";const b1=t.match(/(\d+)\s*(?:Bed|bed|Beds|beds)/i);if(b1)beds=Math.min(+b1[1],12);const b2=t.match(/(\d+)\s*(?:Bath|bath|Baths|baths)/i);if(b2)baths=Math.min(+b2[1],8);const b3=t.match(/(\d+)\s*(?:Car|car|Cars|cars|Park|park|Garag)/i);if(b3)cars=Math.min(+b3[1],8);if(beds===0){const seqMatch=t.match(/(?:^|[\s·•])(\d{1,2})\s+(\d{1,2})\s+(\d{1,2})\s/);if(seqMatch){beds=Math.min(+seqMatch[1],12);baths=Math.min(+seqMatch[2],8);cars=Math.min(+seqMatch[3],8)}}if(beds===0){const nums=[];const parts=t.split(/[\s·•,\|]+/);for(const p of parts){const trimmed=p.trim();if(/^\d{1,2}$/.test(trimmed)&&+trimmed>0&&+trimmed<=12)nums.push(+trimmed);if(/m²|House|Unit|Duplex|Townhouse|Apartment|Villa|Semi/i.test(trimmed))break}if(nums.length>=3){beds=nums[0];baths=nums[1];cars=nums[2]}else if(nums.length===2){beds=nums[0];baths=nums[1]}else if(nums.length===1){beds=nums[0]}}const lm=t.match(/(\d{2,4})(?:\.\d+)?\s*m²/g);if(lm){for(const l of lm){const fval=parseFloat(l.match(/[\d.]+/)[0]);const landVal=Math.round(fval);if(landVal>=50&&landVal<=5000){land=landVal;break}}}for(const k of["Apartment","Unit","Townhouse","Villa","Terrace","Studio","House"]){if(t.toLowerCase().includes(k.toLowerCase())){type=k;break}}return{beds,baths,cars,land,type}}function removeAll(){document.querySelectorAll(".mfi-bar,.mfi-detail-panel").forEach(el=>el.remove())}function scoreColor(score){if(score>=80)return{bg:"#059669",text:"#ecfdf5"};if(score>=65)return{bg:"#10b981",text:"#ecfdf5"};if(score>=50)return{bg:"#f59e0b",text:"#451a03"};return{bg:"#ef4444",text:"#fef2f2"}}async function handleSearch(){const urlSuburb=detectSuburbFromUrl();if(!urlSuburb)return;const subData=await fetchSuburbData(urlSuburb.name,urlSuburb.postcode);let cards=[];if(site==="domain"){document.querySelectorAll('[data-testid^="listing-card-wrapper"]').forEach(c=>cards.push(c));if(!cards.length)document.querySelectorAll('li[class*="listing"]').forEach(c=>{if(!cards.includes(c))cards.push(c)})}if(site==="rea"){document.querySelectorAll("article").forEach(el=>{if(el.querySelector('a[href*="/property-"]'))cards.push(el)});if(!cards.length){document.querySelectorAll('a[href*="/property-"]').forEach(l=>{let p=l.parentElement;for(let i=0;i<6&&p;i++){const cn=(p.className||"").toLowerCase();if(p.tagName==="ARTICLE"||cn.includes("card")||cn.includes("listing")||cn.includes("result")){if(!cards.includes(p))cards.push(p);break}p=p.parentElement}})}if(!cards.length){document.querySelectorAll('a[href*="/property-"]').forEach(l=>{const p=l.closest("div");if(p&&!cards.includes(p)&&p.textContent.length>30&&p.textContent.length<3000)cards.push(p)})}}console.log(`[MFI] Search: ${cards.length} cards`);const cardQueue=[];for(const card of cards){if(card.dataset.mfiDone)continue;card.dataset.mfiDone="1";const text=card.textContent||"";let cardSuburb=null;for(const p of SUBURB_PATTERNS){if(p.regex.test(text)){cardSuburb={name:p.name,postcode:p.postcode};break}}if(!cardSuburb)cardSuburb=urlSuburb;let cardSubData=subData;if(cardSuburb.name!==urlSuburb.name)cardSubData=await fetchSuburbData(cardSuburb.name,cardSuburb.postcode);let price=parsePrice(text);let isEstimated=false;const f=extractFeatures(text);if(!price){const estByBed=cardSubData?.estByBed||{};price=estByBed[Math.min(Math.max(f.beds||3,1),6)]||cardSubData?.house?.medianPrice||900000;if(/unit|apartment|studio|townhouse|villa/i.test(f.type))price=Math.round(price*0.75);isEstimated=true}if(!price)continue;cardQueue.push({card,cardSuburb,price,f,isEstimated,cardSubData})}console.log(`[MFI] Processing ${cardQueue.length} cards`);await Promise.allSettled(cardQueue.map(async({card,cardSuburb,price,f,isEstimated,cardSubData})=>{const apiResult=await fetchAnalysis(cardSuburb.name,cardSuburb.postcode,price,f.beds,f.baths,f.land,f.type);if(!apiResult?.analysis)return;const a=apiResult.analysis;const sc=scoreColor(a.score);const subName=cardSubData?.name||cardSuburb.name.split(" ").map(w=>w[0].toUpperCase()+w.slice(1)).join(" ");const bar=document.createElement("div");bar.className="mfi-bar";bar.innerHTML=`<div class="mfi-bar-score" style="background:${sc.bg};color:${sc.text}"><div class="mfi-bar-score-num">${a.score}</div><div class="mfi-bar-score-label">${a.verdict}</div></div><div class="mfi-bar-metrics"><div class="mfi-bar-metric"><div class="mfi-bar-metric-val">${a.modelPrice?fmt(a.modelPrice):fmt(price)}</div><div class="mfi-bar-metric-label">Est. Price</div></div><div class="mfi-bar-metric"><div class="mfi-bar-metric-val">$${a.rent}/wk</div><div class="mfi-bar-metric-label">Est. Rent</div></div><div class="mfi-bar-metric"><div class="mfi-bar-metric-val">${a.grossYield}%</div><div class="mfi-bar-metric-label">Gross Yield</div></div><div class="mfi-bar-metric"><div class="mfi-bar-metric-val">${fmt(a.proj5)}</div><div class="mfi-bar-metric-label">5yr Projection</div></div><div class="mfi-bar-metric"><div class="mfi-bar-metric-val">${a.dom} days</div><div class="mfi-bar-metric-label">Days on Market</div></div></div>${isEstimated?'<div class="mfi-bar-est">⚡ Price estimated</div>':''}<div class="mfi-bar-brand">${subName} · SW Sydney Property Investor</div>`;card.after(bar)}))}async function handleListing(){if(document.querySelector(".mfi-detail-panel"))return true;const suburbInfo=detectSuburbFromUrl();if(!suburbInfo)return false;const subData=await fetchSuburbData(suburbInfo.name,suburbInfo.postcode);const subName=subData?.name||suburbInfo.name.split(" ").map(w=>w[0].toUpperCase()+w.slice(1)).join(" ");const pageText=document.body.textContent||"";let price=null,listedPriceStr="";document.querySelectorAll('[class*="rice"],[class*="Price"],[data-testid*="price"]').forEach(el=>{if(price)return;const t=el.textContent.trim();if(t.includes("$")&&t.length<60){price=parsePrice(t);if(price)listedPriceStr=t}});if(!price){document.querySelectorAll("h1,h2,h3,p,span").forEach(el=>{if(price)return;const t=el.textContent.trim();if(t.includes("$")&&t.length<60&&t.match(/\$\s*[\d]{1,3}(?:,\d{3}){1,2}/)&&el.children.length<=2){price=parsePrice(t);if(price)listedPriceStr=t}})}if(!price){const m=pageText.match(/\$\d{1,3}(?:,\d{3}){1,2}\s*[-–]\s*\$\d{1,3}(?:,\d{3}){1,2}/);if(m){price=parsePrice(m[0]);listedPriceStr=m[0]}}if(price&&(price<100000||price>5000000))price=null;let address="";const h1=document.querySelector("h1");if(h1)address=h1.textContent.trim();let f={beds:0,baths:0,cars:0,land:0,type:"House"};if(site==="rea"){document.querySelectorAll('[class*="feature"],[class*="Feature"],[class*="property-info"],[aria-label]').forEach(el=>{const text=el.textContent.trim().toLowerCase();const aria=(el.getAttribute("aria-label")||"").toLowerCase();const combined=text+" "+aria;const num=parseInt(text.replace(/[^\d]/g,""))||0;if(num>0&&num<=12){if(combined.includes("bed")&&f.beds===0)f.beds=num;else if(combined.includes("bath")&&f.baths===0)f.baths=num;else if((combined.includes("car")||combined.includes("parking")||combined.includes("garage"))&&f.cars===0)f.cars=num}});if(f.beds===0){const headerArea=document.querySelector('[class*="summary"],[class*="Summary"],[class*="highlights"]');if(headerArea){const ht=headerArea.textContent;const bm=ht.match(/(\d+)\s*(?:Bed|bed)/i);if(bm)f.beds=Math.min(+bm[1],12);const btm=ht.match(/(\d+)\s*(?:Bath|bath)/i);if(btm)f.baths=Math.min(+btm[1],8)}}document.querySelectorAll('[class*="feature"],[class*="size"]').forEach(el=>{if(f.land>0)return;const lm=el.textContent.match(/(\d{2,4})(?:\.\d+)?\s*m²/);if(lm){const val=parseFloat(lm[0]);if(val>=50&&val<=5000)f.land=Math.round(val)}})}if(site==="domain"){document.querySelectorAll('[data-testid*="feature"],[data-testid*="bed"],[data-testid*="bath"],[data-testid*="car"]').forEach(el=>{const text=el.textContent.trim().toLowerCase();const testid=(el.getAttribute("data-testid")||"").toLowerCase();const num=parseInt(text.replace(/[^\d]/g,""))||0;if(num>0&&num<=12){if(testid.includes("bed")&&f.beds===0)f.beds=num;else if(testid.includes("bath")&&f.baths===0)f.baths=num;else if(testid.includes("car")&&f.cars===0)f.cars=num}})}if(f.beds===0){let nearHeader="";const h1El=document.querySelector("h1");if(h1El){let el=h1El;for(let i=0;i<5&&el;i++){el=el.nextElementSibling;if(el)nearHeader+=" "+el.textContent}}nearHeader=nearHeader.substring(0,800);const tempF=extractFeatures(nearHeader);if(tempF.beds>0)f.beds=tempF.beds;if(tempF.baths>0)f.baths=tempF.baths;if(tempF.cars>0)f.cars=tempF.cars;if(tempF.land>0)f.land=tempF.land;if(tempF.type!=="House")f.type=tempF.type}if(f.type==="House"){const urlLower=location.href.toLowerCase();if(urlLower.includes("/property-unit"))f.type="Unit";else if(urlLower.includes("/property-apartment"))f.type="Apartment";else if(urlLower.includes("/property-townhouse"))f.type="Townhouse";else if(urlLower.includes("/property-villa"))f.type="Villa"}console.log(`[MFI] Listing: ${f.beds}bed/${f.baths}bath/${f.cars}car, ${f.land}m², ${f.type}, price=${price}`);let isEstimated=false;if(!price){const estByBed=subData?.estByBed||{};price=estByBed[Math.min(Math.max(f.beds||3,1),6)]||subData?.house?.medianPrice||900000;if(/unit|apartment|studio|townhouse|villa/i.test(f.type))price=Math.round(price*0.75);isEstimated=true;listedPriceStr="Not disclosed"}if(!price)return false;const apiResult=await fetchAnalysis(suburbInfo.name,suburbInfo.postcode,price,f.beds,f.baths,f.land,f.type);const a=apiResult?.analysis;if(!a)return false;const sc=scoreColor(a.score);const panel=document.createElement("div");panel.className="mfi-detail-panel";panel.innerHTML=`<div class="mfi-panel-header"><span>🏠 ${subName} Investor</span><button class="mfi-panel-close">✕</button></div>${address?`<div class="mfi-panel-addr">${address}</div>`:""}<div class="mfi-panel-score" style="background:${sc.bg};color:${sc.text}"><div class="mfi-panel-score-num">${a.score}/100</div><div class="mfi-panel-score-label">${a.verdict}</div></div><div class="mfi-panel-metrics"><div class="mfi-panel-metric"><div class="mfi-panel-metric-val">${a.modelPrice?fmt(a.modelPrice):fmt(price)}</div><div class="mfi-panel-metric-label">Est. Price</div></div><div class="mfi-panel-metric"><div class="mfi-panel-metric-val">$${a.rent}/wk</div><div class="mfi-panel-metric-label">Est. Rent</div></div><div class="mfi-panel-metric"><div class="mfi-panel-metric-val">${a.grossYield}%</div><div class="mfi-panel-metric-label">Gross Yield</div></div><div class="mfi-panel-metric"><div class="mfi-panel-metric-val">${fmt(a.proj5)}</div><div class="mfi-panel-metric-label">5yr Projection</div></div><div class="mfi-panel-metric"><div class="mfi-panel-metric-val">${a.dom} days</div><div class="mfi-panel-metric-label">Days on Market</div></div></div>${isEstimated?'<div class="mfi-panel-est">⚡ Price estimated from suburb median</div>':''}<div class="mfi-panel-footer">${subName} ${suburbInfo.postcode} · Median ${fmt(subData?.house?.medianPrice||0)} · Growth ${a.growth}% p.a. (modelled)<br>Source: ML Model + CoreLogic · Not financial advice</div>`;document.body.appendChild(panel);panel.querySelector(".mfi-panel-close").addEventListener("click",()=>panel.remove());let dragging=false,ox,oy;const hdr=panel.querySelector(".mfi-panel-header");hdr.style.cursor="grab";hdr.addEventListener("mousedown",e=>{dragging=true;hdr.style.cursor="grabbing";const r=panel.getBoundingClientRect();ox=e.clientX-r.left;oy=e.clientY-r.top;e.preventDefault()});document.addEventListener("mousemove",e=>{if(!dragging)return;panel.style.left=(e.clientX-ox)+"px";panel.style.top=(e.clientY-oy)+"px";panel.style.right="auto";panel.style.bottom="auto"});document.addEventListener("mouseup",()=>{dragging=false;hdr.style.cursor="grab"});return true}function route(){const url=location.href;if(url===lastUrl)return;lastUrl=url;removeAll();document.querySelectorAll("[data-mfi-done]").forEach(el=>delete el.dataset.mfiDone);if(isSearchPage(url)){setTimeout(()=>handleSearch(),2500);setTimeout(()=>handleSearch(),5000);setTimeout(()=>handleSearch(),8000)}else if(isListingPage(url)){let att=0;const go=()=>{if(document.querySelector(".mfi-detail-panel"))return;att++;handleListing().then(ok=>{if(!ok&&att<8)setTimeout(go,1500)})};setTimeout(go,2500)}}const _p=history.pushState,_r=history.replaceState;history.pushState=function(){_p.apply(this,arguments);setTimeout(route,500)};history.replaceState=function(){_r.apply(this,arguments);setTimeout(route,500)};window.addEventListener("popstate",()=>setTimeout(route,500));setInterval(()=>{if(location.href!==lastUrl)route()},2000);let sd=null;new MutationObserver(()=>{if(isSearchPage()){clearTimeout(sd);sd=setTimeout(()=>handleSearch(),2000)}}).observe(document.body,{childList:true,subtree:true});route();if(typeof chrome!=="undefined"&&chrome.runtime?.onMessage){chrome.runtime.onMessage.addListener((m,s,r)=>{if(m.action==="scrape")r({site,bars:document.querySelectorAll(".mfi-bar").length});return true})}})();
